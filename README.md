# Лабораторна робота №6: Взаємодія фронтенду та бекенду

### **Дисципліна:** Web-програмування  
### **Тема:** Організація взаємодії фронтенду та бекенду за допомогою JavaScript та PHP

**Варіант:** 13  
**Виконала:** Мальцева Софія, група ІП-41  

---

## 🎯 Мета роботи

1. **Ознайомитися з принципами взаємодії фронтенду та бекенду** у вебзастосунках.
2. Навчитися використовувати **JavaScript та PHP** для асинхронного обміну даними між клієнтом і сервером.
3. Реалізувати **збереження та отримання даних** на сервері без перезавантаження сторінки.
4. Закріпити навички динамічного формування HTML-структур за даними, отриманими з бекенду.

---

## 🛠 Умови та реалізація

Для виконання лабораторної роботи було використано вебсторінки з **комп’ютерного практикуму №3** як основу інтерфейсу.

Згідно з **варіантом 13**, реалізовано **набір об’єктів типу “Dropdown”** (горизонтальне меню з випадаючими пунктами).

### Функціональні вимоги:

- створення довільної кількості пунктів меню;
- створення вкладених (випадаючих) підпунктів;
- можливість задавати назву та посилання для кожного пункту;
- асинхронне збереження структури меню на сервері;
- асинхронне отримання та відображення меню на іншій сторінці;
- автоматичне оновлення меню при зміні даних на сервері;
- **без використання JS- та CSS-фреймворків**.

---

## 📄 Структура проєкту

Робота складається з **двох вебсторінок**:

### 🔹 Перша сторінка — конструктор меню (`page1.html`)

- У центральному блоці сторінки розміщено **форму для створення меню**.
- Користувач може:
  - додавати пункти верхнього рівня;
  - додавати вкладені підпункти;
  - вказувати назву та URL для кожного елемента.
- Після заповнення структура меню **асинхронно передається на сервер** за допомогою `fetch` (HTTP POST).
- Дані зберігаються на сервері у форматі **JSON**.

### 🔹 Друга сторінка — відображення меню (`page2.html`)

- При завантаженні сторінки за допомогою JavaScript виконується **асинхронний запит до сервера**.
- Отримані дані використовуються для **динамічного формування HTML-коду горизонтального Dropdown-меню**.
- Реалізовано **періодичну перевірку змін** (polling), завдяки чому меню оновлюється автоматично без перезавантаження сторінки.

---

## ⚙️ Серверна частина

Для серверної частини використано **вбудований сервер PHP (PHP Built-in Web Server)**.

### Серверні скрипти:

- `save_data.php` — приймає POST-запит з JSON-даними та зберігає структуру меню у файл `menu_data.json`;
- `get_menu.php` — повертає збережені дані меню у форматі JSON для клієнтської частини.

### Формат збереження даних:
- JSON-файл (`menu_data.json`), що містить структуру пунктів меню та вкладених елементів.

---

## 🔄 Асинхронна взаємодія

У роботі використано:
- **Fetch API** для виконання асинхронних HTTP-запитів;
- метод **POST** для збереження даних;
- метод **GET** для отримання даних;
- оновлення інтерфейсу **без перезавантаження сторінки**.

---

## 🖥 Запуск проєкту

Для запуску проєкту необхідно у директорії проекту виконати команду:

```bash
php -S 127.0.0.1:8000
```
та відкрити у браузері сторінки
http://localhost:8000/page1.html
та http://localhost:8000/page2.html відповідно